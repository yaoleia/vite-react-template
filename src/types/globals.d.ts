interface Window {
  // 给这个属性申明一下
  __REDUX_DEVTOOLS_EXTENSION_COMPOSE__?: any;
  AMap: any;
}

interface Date {
  Format(parps: string): string;
}

interface loading {
  loading: boolean;
}
interface className {
  className?: string;
}
